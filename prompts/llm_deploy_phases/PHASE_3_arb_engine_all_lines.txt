# PHASE 3 of 6 — Code: arb engine for ALL bet types × ALL books × ALL sports

Prerequisite: PHASE 2 ingests multi-sport multi-market data.

REFERENCE: prompts/llm_deploy_phases/ALL_SPORTS_AND_MARKETS.txt
SHARED RULES: prompts/llm_deploy_phases/_SHARED_RULES.txt

GOAL: Score arbitrage on every line type across every US book, every sport — not moneyline-only, not NBA-only.

FILES TO EDIT:
- harvester/engine.py — per-market detectors (h2h, spreads, alt spreads, totals, alt totals, props, outrights where 2-way)
- harvester/dashboard_service.py — opportunities[] with sport_key, market_type, line/point, legs
- harvester/llm_arbitrage.py — keep all TARGET_SOURCES books; log zero-quote books

Matching rules:
- h2h/outrights: best price per outcome across books
- spreads/alternate_spreads: same team + same point
- totals/alternate_totals: same point, over vs under
- player props: player + stat + point (never cross mismatched lines)
- soccer BTTS / DNB: standard two-way rules

Verify all sports:

```bash
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline
source venv/Scripts/activate
python arbitrage_orchestrator.py --full
python -c "
import json; from pathlib import Path
from collections import Counter
d=json.loads(Path('../harvester/data/ingestion_snapshot.json').read_text())
print('sports_with_events', Counter(e.get('sport_key') for e in d.get('events',[])))
print('arb_opportunities', sum(1 for e in d.get('events',[]) if e.get('arbitrage')))
"
```

EXPECT: multiple sports in output; engine completes without NBA-only filter.

STOP for output. Next: PHASE_4_dashboard_lan_url.txt
