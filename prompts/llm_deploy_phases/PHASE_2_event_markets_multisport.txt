# PHASE 2 of 6 — Code: event-level markets + multi-sport loop

Prerequisite: PHASE 1 passed (lines >> 2). Implement deep Odds API fetch for alternates + props; loop all leagues.

SHARED RULES: prompts/llm_deploy_phases/_SHARED_RULES.txt

GOAL: Fetch alternate_spreads, alternate_totals, player props across NBA/NFL/MLB/NHL.

FILES TO EDIT:
- harvester/integrators/odds_api.py (add events list + per-event /events/{id}/odds)
- harvester/odds_api_fetch.py (merge deep markets into UnifiedRecord)
- harvester/settings.py (ODDS_API_DEEP_MARKETS, ODDS_API_EVENT_MARKETS, ODDS_API_SPORTS, ODDS_API_MAX_EVENTS_PER_SPORT)
- harvester/arbitrage_orchestrator.py (loop ODDS_API_SPORTS)

Append to harvester/.env (do NOT change ODDS_API_KEY line):

```env
ODDS_API_DEEP_MARKETS=1
ODDS_API_EVENT_MARKETS=alternate_spreads,alternate_totals,player_points,player_rebounds,player_assists,player_threes,player_points_alternate,player_rebounds_alternate,player_assists_alternate
ODDS_API_SPORTS=basketball_nba,americanfootball_nfl,baseball_mlb,icehockey_nhl
ODDS_API_MAX_EVENTS_PER_SPORT=10
```

Market keys reference: h2h, spreads, totals (bulk); alternates + props (event endpoint).

WARN USER: 10 games × 15 markets ≈ 150 API credits per sport per refresh.

After code: one verify command:

```bash
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline
source venv/Scripts/activate
python arbitrage_orchestrator.py --sport basketball_nba --markets h2h spreads totals --full
```

EXPECT: higher line counts; player_prop or alternate_* keys in snapshot JSON.

STOP for output. Next: PHASE_3_arb_engine_all_lines.txt
