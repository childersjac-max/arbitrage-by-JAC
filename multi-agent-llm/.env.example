# Ollama API
OLLAMA_URL=http://127.0.0.1:11434/api/generate

# --- 3-agent models (16GB RAM / CPU: 3B planner + 7B coder + 3B reviewer) ---
MODEL_PLANNER=qwen2.5:3b-instruct-q4_K_M
MODEL_CODER=qwen2.5-coder:7b-instruct-q4_K_M
MODEL_REVIEWER=qwen2.5:3b-instruct-q4_K_M

# Legacy names still work: MULTI_AGENT_MODEL_A / MULTI_AGENT_MODEL_B

MAX_LOOPS=3
DELAY_BETWEEN_CALLS=2
MIN_SCORE_REGENERATE=6

# Streaming tokens to terminal (1=on, 0=off)
STREAM_OUTPUT=1

# Timeouts (seconds) — raise CODER if 7B is slow on CPU
TIMEOUT_PLANNER_SEC=120
TIMEOUT_CODER_SEC=600
TIMEOUT_REVIEWER_SEC=180

# Default task if you run with no arguments
MULTI_AGENT_TASK=Write a Python function to validate email addresses

# Set PROMPT_INTERACTIVE=1 to always ask "Enter task:" at runtime
