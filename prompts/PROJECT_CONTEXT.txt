# PROJECT CONTEXT (always active)
# User: child @ DESKTOP-R9J0LKD | Shell: Git Bash | Project: sports arbitrage pipeline

You ALWAYS work on the **arbitrage-by-JAC** sports arbitrage monorepo. Never greenfield.

## Identity
- Project: Sports arbitrage ingestion + dashboard + local LLM operator tools
- Repo: arbitrage-by-JAC (GitHub: childersjac-max/arbitrage-by-JAC)
- User machine: Windows 11, 16GB RAM, CPU-only Ollama (no NVIDIA GPU)

## App URLs (open in browser)
- Local LLM + multi-agent chat: APP_URL_LOCAL_LLM (default http://127.0.0.1:7860)
- Phone/tablet on same Wi-Fi: APP_URL_LOCAL_LLM_LAN (set in .env after running doctor)
- Harvester arbitrage dashboard: APP_URL_HARVESTER (default http://127.0.0.1:8765)
- Public tunnel (optional Gradio share): APP_URL_PUBLIC (set when LOCAL_LLM_UI_SHARE=1)

## Path roots (use these in every command and code path)
- Git Bash root: ~/Projects/arbitrage-by-JAC
- Windows root: C:\Users\child\Projects\arbitrage-by-JAC

## Hard rules
1. Edit EXISTING files under the repo — do not invent arbitrage_core_engine/, stealth_session.py, or Playwright book scrapers.
2. Lawful odds: The Odds API via harvester/integrators/odds_api.py
3. Polymarket: harvester/integrators/polymarket.py (public Gamma API, optional)
4. Windows venv: source venv/Scripts/activate or source ./activate_venv.sh — NEVER venv/bin/activate
5. Port 7860 = local-llm | Port 8765 = harvester dashboard
6. Success = user pasted REAL terminal output from commands you gave — not generated Python alone

## Default run commands (copy exactly)
Orchestrator:
  cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline && source venv/Scripts/activate && python arbitrage_orchestrator.py --sport basketball_nba

Harvester dashboard:
  cd ~/Projects/arbitrage-by-JAC/harvester && source ../sports_arbitrage_pipeline/venv/Scripts/activate && python web_app.py

Local LLM (multi-agent in browser):
  cd ~/Projects/arbitrage-by-JAC/local-llm && source .venv/Scripts/activate && python web_app.py

## API keys
- ODDS_API_KEY in harvester/.env (never ask user to paste key in chat)
- Ollama: no key; must be running in system tray
