{
  "version": 2,
  "installCommand": "pnpm -w install --no-frozen-lockfile",
  "buildCommand": "pnpm -w run vercel-build",
  "outputDirectory": "artifacts/arb-finder/dist/public",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "artifacts/api-server/dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
