# PHASE 6 of 6 — Always-on multi-sport refresh

REFERENCE: prompts/llm_deploy_phases/ALL_SPORTS_AND_MARKETS.txt
SHARED RULES: prompts/llm_deploy_phases/_SHARED_RULES.txt

GOAL: Auto-refresh **all active sports** + **all configured markets** on a schedule.

OPTION A — env loop in dashboard_service.py:
```env
HARVESTER_AUTO_REFRESH_MIN=15
ODDS_API_SPORTS_MODE=all_active
ODDS_API_MAX_SPORTS_PER_RUN=50
```

OPTION B — Task Scheduler runs:
```bash
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline && source venv/Scripts/activate && python arbitrage_orchestrator.py --full
```

Verify snapshot updates across sports:

```bash
python -c "
import json; from pathlib import Path
from collections import Counter
d=json.loads(Path('harvester/data/ingestion_snapshot.json').read_text())
print('sports', Counter(e.get('sport_key') for e in d.get('events',[])))
print('total_lines', sum(len(e.get('lines',[])) for e in d.get('events',[])))
"
```

DONE — full multi-sport arbitrage pipeline operational.
