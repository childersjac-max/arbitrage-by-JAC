# PHASE 5 of 6 — Verify in browser (commands + user checklist)

Prerequisite: PHASE 4 dashboard runs. No new code unless refresh fails.

SHARED RULES: prompts/llm_deploy_phases/_SHARED_RULES.txt

GOAL: User sees real odds and arb opportunities at :8765.

COMMAND:

```bash
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline
source venv/Scripts/activate
python arbitrage_orchestrator.py --sport basketball_nba --markets h2h spreads totals --full
cd ../harvester
python web_app.py
```

USER CHECKLIST (ask user to confirm yes/no):
1. Open http://127.0.0.1:8765 on PC
2. Sources tab: DraftKings, FanDuel, BetMGM, Caesars, Fanatics show lines > 2
3. Click Refresh run — completes without API key error
4. Opportunities tab loads (arbs may be empty — that is OK)
5. Phone on same Wi-Fi: http://LAN_IP:8765 works

Optional API check:

```bash
curl -s http://127.0.0.1:8765/api/status | head -c 500
```

EXPECT: api_key_configured true; events > 0; source rows populated

STOP when user confirms browser works. Next: PHASE_6_always_on_optional.txt
