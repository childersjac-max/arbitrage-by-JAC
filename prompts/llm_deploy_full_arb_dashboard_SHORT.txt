Harvester engineer — ~/Projects/arbitrage-by-JAC. Use 6 phase files in prompts/llm_deploy_phases/ (paste ONE per session). Full spec: llm_deploy_full_arb_dashboard.txt. Setup guide: llm_deploy_phases/00_START_HERE_MULTI_AGENT.md

User wants: (1) ALL US books + ALL line types for arb, (2) deploy results app with URL (:8765).

ODDS API KEY: Always in ~/Projects/arbitrage-by-JAC/harvester/.env (ODDS_API_KEY=). Auto-loaded every run. NEVER ask user to paste/confirm key. Key rotation = user edits .env in Notepad only. Verify: sports_arbitrage_pipeline venv → cd ../harvester → python cli.py health. NOT local-llm/.venv.

BOOKS (Odds API us region): draftkings, fanduel, betmgm, caesars/williamhill_us, fanatics, thescore/espnbet — keep via filter_records_to_target_sources in llm_arbitrage.py. Direct-only stubs (kalshi, polymarket, bet365) = "Not connected" until official API.

LINES (must fetch + score): h2h, spreads, alternate_spreads, totals, alternate_totals, player_points/rebounds/assists/threes, player_*_alternate. Bulk /odds = featured only; props+alts need per-event /events/{id}/odds. Extend engine.py beyond detect_h2h_arbitrage — match spreads/totals/props on same line/point.

SPORTS: ALL active via GET /v4/sports (ODDS_API_SPORTS_MODE=all_active). NOT NBA-only. Ref: prompts/llm_deploy_phases/ALL_SPORTS_AND_MARKETS.txt

MARKETS: h2h, spreads, totals, outrights + alternate_spreads/totals + sport-specific player props (max lines for arb).

DASHBOARD: :8765 — must show all sports. See phase files in prompts/llm_deploy_phases/

PHASE 1 (now): multi-sport ingest — see PHASE_1_harvester_config_ingest.txt (loops all active sports × h2h spreads totals outrights).

PHASE 2: event markets + multi-sport loop + per-market arb in engine.py + dashboard opportunities. See prompts/llm_deploy_full_arb_dashboard.txt

PHASE 3: LAN bind + startup script. User opens :8765 on PC/phone.

Rules: harvester/ only; no scraping; one command/reply; warn API credits; key stays in harvester/.env — never re-ask.

START: python cli.py health (harvester/), then PHASE 1. No key questions.
