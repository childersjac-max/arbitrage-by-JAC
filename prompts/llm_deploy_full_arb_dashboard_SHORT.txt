Harvester engineer — ~/Projects/arbitrage-by-JAC. User wants: (1) ALWAYS scan ALL US books + ALL line types for arbitrage, (2) deploy a live results app with a URL they can open.

ODDS API KEY: Already in harvester/.env. NEVER re-ask. Verify: cd sports_arbitrage_pipeline && source venv/Scripts/activate && cd ../harvester && python cli.py health. Use sports_arbitrage_pipeline/venv NOT local-llm/.venv (fixes pydantic_settings error).

BOOKS (Odds API us region): draftkings, fanduel, betmgm, caesars/williamhill_us, fanatics, thescore/espnbet — keep via filter_records_to_target_sources in llm_arbitrage.py. Direct-only stubs (kalshi, polymarket, bet365) = "Not connected" until official API.

LINES (must fetch + score): h2h, spreads, alternate_spreads, totals, alternate_totals, player_points/rebounds/assists/threes, player_*_alternate. Bulk /odds = featured only; props+alts need per-event /events/{id}/odds. Extend engine.py beyond detect_h2h_arbitrage — match spreads/totals/props on same line/point.

LEAGUES: basketball_nba, americanfootball_nfl, baseball_mlb, icehockey_nhl (env ODDS_API_SPORTS).

DASHBOARD URL: harvester/web_app.py port 8765 (NOT 7860 chat). Add HARVESTER_UI_LAN=1, HARVESTER_UI_HOST=0.0.0.0, network_urls.py (copy from local-llm), print http://127.0.0.1:8765 + http://LAN_IP:8765. Optional public: cloudflared tunnel → APP_URL_PUBLIC. Script: harvester/scripts/run_dashboard.sh runs orchestrator then web_app.

PHASE 1 (now): harvester/.env ODDS_API_MARKETS=h2h,spreads,totals. Run:
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline && source venv/Scripts/activate && python arbitrage_orchestrator.py --sport basketball_nba --markets h2h spreads totals --full
Verify lines >> 2. STOP for output.

PHASE 2: event markets + multi-sport loop + per-market arb in engine.py + dashboard opportunities. See prompts/llm_deploy_full_arb_dashboard.txt

PHASE 3: LAN bind + startup script. User opens :8765 on PC/phone.

Rules: harvester/ only; no scraping; one command/reply; warn API credits; key stays in harvester/.env — never re-ask.

START: python cli.py health (harvester/), then PHASE 1. No key questions.
