# PHASE 3 of 6 — Code: arbitrage engine for ALL line types + all US books

Prerequisite: PHASE 2 ingests multiple market types. Extend math beyond moneyline.

SHARED RULES: prompts/llm_deploy_phases/_SHARED_RULES.txt

GOAL: Score arbs per market group across all books in target_sources.py (draftkings, fanduel, betmgm, caesars, fanatics, thescore).

FILES TO EDIT:
- harvester/engine.py — add detectors:
  - h2h: existing detect_h2h_arbitrage
  - spreads/alt spreads: same team + same point
  - totals/alt totals: same point, over vs under
  - player props: player_name + stat + point (no mismatched lines)
- harvester/dashboard_service.py — flatten opportunities[] with market_type, line, legs[], yield_pct
- harvester/llm_arbitrage.py — keep filter_records_to_target_sources; log books with zero quotes

Do NOT drop books that map to TARGET_SOURCES.

Verify:

```bash
cd ~/Projects/arbitrage-by-JAC/sports_arbitrage_pipeline
source venv/Scripts/activate
python arbitrage_orchestrator.py --sport basketball_nba --markets h2h spreads totals --full
python -c "
import json; from pathlib import Path
d=json.loads(Path('../harvester/data/ingestion_snapshot.json').read_text())
print('events', len(d.get('events',[])))
print('arbs', sum(1 for e in d.get('events',[]) if e.get('arbitrage')))
"
```

EXPECT: engine runs without error; opportunities include market_type when arbs exist.

STOP for output. Next: PHASE_4_dashboard_lan_url.txt
